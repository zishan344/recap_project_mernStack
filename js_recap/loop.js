{
  let number = 1;
  while (number <= 10) {
    // console.log("starting loop..." + number);
    number++;
  }

  const arr = [5, 10, 15, 20, 25, 30, 35, 40, 50];

  for (let i = 0; i < arr.length; i++) {
    // console.log(arr[i]);
  }
}
{
  // problem in loop
  // 1 even number
  let i = 0;
  while (i < 400) {
    // console.log(i);
    i += 2;
  }
  let oddNumber = 1;
  while (oddNumber < 400) {
    // console.log(oddNumber);
    oddNumber += 2;
  }
  // reverse numbers
  let reverse = 10;
  while (reverse >= 1) {
    console.log(reverse);
    reverse--;
  }
}
